[Service]
ExecStart=
ExecStart=/usr/sbin/varnishd -a :{{ varnish_port | default('80') }} \
  -T localhost:6082 \
  -f /etc/varnish/default.vcl \
  -S /etc/varnish/secret \
  -s malloc,256M

[Install]
WantedBy=multi-user.target
